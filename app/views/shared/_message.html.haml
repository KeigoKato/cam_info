- flash.each do |key, value|
  - key = "info" if key == "notice"
  - key = "danger"  if key == "alert"
  -# = content_tag :div, "#{key} : #{value}", class: "alert alert-#{key}"
  = content_tag :div, "#{key} : #{value}", class: "text-center alert-#{key}", style: "font-size: 18px"
